source_dir: .git/hooks

pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    lint:
      run: npm run lint
      stage_fixed: true
    format:
      run: npx prettier --check .
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  parallel: false # Sequencial para garantir ordem correta
  commands:
    validate-basics:
      run: |
        echo "🔍 Validação básica..."
        npm run validate
    test:
      run: |
        echo "🧪 Rodando testes..."
        npm test -- --passWithNoTests
        TEST_EXIT_CODE=$?

        if [ $TEST_EXIT_CODE -ne 0 ]; then
          echo "⚠️  Testes falharam. Tentando atualizar snapshots..."
          npm run test:snapshots
          SNAPSHOT_EXIT_CODE=$?

          if [ $SNAPSHOT_EXIT_CODE -eq 0 ]; then
            echo "✅ Snapshots atualizados com sucesso!"
            git add "**/__snapshots__/**" 2>/dev/null || true
            echo "📸 Snapshots adicionados ao commit"
            exit 0
          else
            echo "❌ TESTES FALHARAM mesmo após atualizar snapshots!"
            echo "❌ Corrija os erros de teste antes de fazer push."
            exit 1
          fi
        else
          echo "✅ Todos os testes passaram!"
          exit 0
        fi
      fail_text: "❌ PUSH BLOQUEADO! Há testes falhando. Corrija antes de fazer push."
    sonar-doublecheck:
      run: |
        echo "🔍 SonarQube Double-Check..."

        # Obter arquivos modificados que ainda não foram commitados
        STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx)$' || true)

        # Obter arquivos modificados em relação ao commit anterior
        MODIFIED_FILES=$(git diff HEAD~1 --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx)$' || true)

        ALL_FILES="$STAGED_FILES $MODIFIED_FILES"

        if [ -z "$ALL_FILES" ]; then
          echo "ℹ️  Nenhum arquivo TS/JS modificado encontrado"
          echo "✅ Prosseguindo sem análise SonarQube"
        else
          echo "📊 Executando npm run sonar:doublecheck..."
          npm run sonar:doublecheck

          echo "📈 Relatório: https://sonarcloud.io/project/overview?id=mayconaraujosantos_pillmind"
          echo "💡 Verifique o Problems Panel (Ctrl+Shift+M) no VS Code para issues detalhadas"
          echo "✅ SonarQube double-check concluído!"
        fi
